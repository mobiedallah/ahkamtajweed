<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mohammadobiedallah">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:permission="INTERNET"
        android:theme="@style/AppTheme">
        <activity android:name=".other"></activity>
        <activity android:name=".maad" />
        <activity android:name=".meem" />
        <activity android:name=".noon" />
        <activity android:name=".Interface">

        </activity>
                <activity android:name=".userReg" />
        <activity android:name=".expertreg" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>